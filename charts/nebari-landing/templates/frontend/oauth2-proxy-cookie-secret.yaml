{{- /*
oauth2-proxy has been removed from the frontend pod (see issue #53).
This file is intentionally empty. It is kept as a placeholder so that
existing Helm release history does not show a spurious deletion diff.
*/ -}}
