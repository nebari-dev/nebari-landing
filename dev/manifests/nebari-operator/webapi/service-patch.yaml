# Strategic merge patch — adds LoadBalancer type and pinned MetalLB IP.
# Keeps the existing port 8080; access webapi at http://192.168.49.101:8080
apiVersion: v1
kind: Service
metadata:
  name: webapi
  namespace: nebari-system
spec:
  type: LoadBalancer
  loadBalancerIP: "192.168.49.101"
